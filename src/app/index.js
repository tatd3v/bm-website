import {
  setEventsData,
  setEventInfo,
  setShowCalendar,
  setShowEvent,
} from './slides';
import { store } from './store';

export { setEventsData, setEventInfo, setShowCalendar, setShowEvent, store };
